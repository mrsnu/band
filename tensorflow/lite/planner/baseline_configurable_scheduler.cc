#include "tensorflow/lite/planner/baseline_configurable_scheduler.h"

namespace tflite {
namespace impl {

void RoundRobinScheduler::Schedule(JobQueue& requests) {
  // Set config from user's runtime_config and schedule following to the config
}

}  // namespace impl
}  // namespace tflite
